import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  safeArea: {
    flex: 1,
    padding: 10
  },
  inner: {
    flex: 1,
  },
  container: {
    padding: 10
  }
});

export default styles;
