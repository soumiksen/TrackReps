import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  circularBar: {
    borderColor: '#E7E7E7',
    borderWidth: 14,
    width: 120,
    height: 120,
    borderRadius: 1000,
  }
})

export default styles;