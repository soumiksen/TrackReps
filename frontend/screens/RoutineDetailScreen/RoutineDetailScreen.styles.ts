import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  title: {
    fontSize: 24,
    textAlign: 'center',
    marginBottom: 16,
    fontWeight: 'bold',
  },
  container: {
    padding: 16,
    flex: 1,
  },
  exerciseList: {
    display: 'flex',
    gap: 10,
  },
});

export default styles;